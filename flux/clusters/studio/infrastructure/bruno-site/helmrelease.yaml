---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bruno-site
  namespace: bruno-site
spec:
  interval: 5m
  chart:
    spec:
      chart: /
      version: 0.1.0
      sourceRef:
        kind: GitRepository
        name: bruno-site-chart
        namespace: flux-system