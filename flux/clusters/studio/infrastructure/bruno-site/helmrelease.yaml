---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bruno-site
  namespace: bruno-site
spec:
  interval: 5m
  chart:
    spec:
      chart: bruno-site-chart
      version: v0.0.1
      sourceRef:
        kind: HelmRepository
        name: bruno-site-chart
        namespace: bruno-site