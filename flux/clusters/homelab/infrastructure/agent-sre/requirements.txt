# Core dependencies
langchain>=0.1.0
langchain-ollama==0.1.3
langsmith>=0.1.0
logfire>=0.1.0

# MCP server dependencies with SSE support
# Note: mcp package requires Python 3.10+, using alternative implementation
# mcp>=1.0.0
# langchain-mcp-adapters>=0.1.0

# HTTP and async support with SSE
httpx>=0.25.0
aiohttp>=3.9.0
aiohttp-sse>=0.1.0

# Data handling
pydantic>=2.0.0
python-dotenv>=1.0.0

# Logging and monitoring
structlog>=23.0.0

# Kubernetes client (for future enhancements)
kubernetes>=28.0.0

# Additional utilities
asyncio-mqtt>=0.16.0
python-json-logger>=2.0.0

# SSE and real-time communication
sse-starlette>=1.6.0
