apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: homelab-infrastructure
  namespace: flux-system

resources:
  - repositories
  - cert-manager
  - linkerd
  - metrics-server
  - prometheus-operator
  - sealed-secrets
  - cloudflare-tunnel
  - homepage
